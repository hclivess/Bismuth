# doc/19 — Decentralized-apps VM (smart contracts)

The on-chain, **post-fork** smart-contract layer. This is the complete map of what exists, what is
tested, and what is still missing — so nothing is assumed done that isn't.

Everything here is **inert until hf2 activates** and behind the `vm` config flag (default **off**; on for
regnet tests). It adds **no behaviour to the current chain**.

## How it fits together

```
vm: transaction (consensus-ordered, signed, in a block)
        │  digest, ONLY when block_height >= node.fork_height  (digest.py)
        ▼
vm_engine.apply_block_rows ──> per tx: parse vm:deploy / vm:call
        │                               dispatch on the contract's engine tag
        ▼
   execution engine            ┌─ bismuth_vm.py     (compact EVM-style bytecode)   ENGINE 0
   (pluggable)                 └─ bismuth_riscv.py  (RV32I RISC-V interpreter)      ENGINE 1
        │  reads/writes
        ▼
   vm_state.py  (LMDB: code + storage)  ──>  vm_state.state_root()  ──> node.vm_state_root
```

## Parts, status, and tests

| Part | File | Status | Test |
|---|---|---|---|
| Deterministic bytecode engine | `bismuth_vm.py` | **done** — int-only 256-bit, gas, JUMPDEST-validated, revert-on-fault | `test_vm.py` (12) |
| Deterministic RISC-V engine (RV32I) | `bismuth_riscv.py` | **done** — ALU/branch/load-store/JAL(R)/LUI/AUIPC, gas, ECALL ABI | `test_riscv.py` (6) |
| Pluggable engine dispatch | `vm_engine.py` | **done** — `riscv:`-prefixed deploy → RISC-V, else bytecode; 1-byte tag | `test_vm_post_fork.py` |
| Contract state store | `vm_state.py` | **done** — LMDB code+storage, rebuildable | `test_vm_state.py`, integration |
| Deploy / call tx format | `vm_engine.py` | **done** — `vm:deploy <hex>` / `vm:call <addr>:<calldata hex>` | `test_vm_post_fork.py` |
| Deterministic contract address | `vm_engine.contract_address` | **done** — blake2b of the deploy signature | integration |
| Fork gate (post-fork only) | `digest.py` | **done** — runs vm: txs iff `block_height >= fork_height` | `test_vm_post_fork.py` |
| `fork_height` caching | `digest.py` | **done** — cached once determinable (also activates the LWMA gate) | `test_vm_post_fork.py` |
| **State root (commitment)** | `vm_state.state_root` | **done** — deterministic blake2b over all code+storage; maintained + exposed | `test_vm_state.py` |
| Reorg safety | `chain_ops.py` | **done** — state + root rebuilt from the rolled-back ledger | (unified rollback path) |
| Explorer API + view | `rest_api.py`, `web/explorer` | **done** — `/api/vm/contracts`, `/api/vm/contract/{addr}` + Contracts view | `test_vm_post_fork.py` |
| HTLC primitives (atomic swaps) | `bismuth_vm` SHA256+NUMBER; `bismuth_riscv` `SYS_SHA256` | **done** — preimage + timelock expressible in both engines | `test_vm.py` |
| **State-root enforcement** | `vm_engine` embed/extract + `regnet` miner + `digest` validator | **done** — coinbase commits the pre-state root (after the hf2 signal); digester REJECTS a mismatch before commit | `test_vm_state.py`, `test_vm_post_fork.py` |
| — | | | |
| **Value custody** (contract holds/moves BIS) | — | **MISSING** — needed for a *usable* HTLC / DeFi; consensus-critical | — |
| **Gas economics** (pricing/fees) | — | **MISSING** — fixed 1M budget today, not metered against fees | — |
| Merkle state trie (inclusion proofs) | — | **MISSING** — root is a flat hash, no light-client proofs / incremental update | — |
| Compiler toolchain (C/Rust → RISC-V deploy) | — | **MISSING** — interpreter runs raw RV32I; no packaged toolchain yet | — |

## Layer model (important)

Execution is **main-layer** (every full node runs it; `vm:` txs are consensus-ordered and final), and the
state root is now **consensus-ENFORCED**: post-fork the miner commits the pre-state root in the coinbase
(after the hf2 signal, PoW-hashed like the rest of the nonce) and the digester **rejects** a block whose
committed root disagrees with its own. So a non-determinism bug is a **caught block-rejection, not a
silent divergence** — main-layer execution *plus* the hash-anchored integrity an L2 hash-commit gives,
with no separate layer or sequencer. See doc/17 for the trade-off.

## Next steps (in order)
1. **Value custody** — a host op / native lock so a contract can hold and release BIS → real HTLC swaps.
2. **Gas economics** — price gas against tx fees; bound state growth.
3. **Merkle trie** — incremental root + inclusion proofs for light clients.
