# Pip3 requirements - synced with requirements-node since wallet is now separate.

# You need to manually run the following line first
# pip3 install simple-crypt --no-deps

# Now the minimal modules needed for a full install

PySocks
pycryptodomex
requests
base58
# polysign is vendored in-tree (see polysign/); it is no longer a pip dependency.
# REQUIRED for a full node (NOT optional): mainnet carries ECDSA and ED25519 transactions, so a node
# missing these rejects every such block ("ModuleNotFoundError" in polysign/signer_*) and silently
# stalls -- it cannot sync past the first ed25519/ecdsa block.
coincurve
# ed25519's sdist build is broken on Python 3.12 (bundled versioneer uses configparser.SafeConfigParser
# / readfp, both removed in 3.12). On 3.12 install from source with setup.py's versioneer.get_version()
# /get_cmdclass() calls replaced by a literal (e.g. "1.5"); older Pythons pip-install it directly.
ed25519
# OPTIONAL (post-quantum, doc/20): pure-Python ML-DSA-65 for polysign/signer_mldsa.py. Lazy-imported by
# the SignerFactory, so a node without it is unaffected until a `pq` fork activates the PQ signature type.
dilithium-py
