# Pip3 requirements for a node only setup

# You need to manually run the following line first
# pip3 install simple-crypt --no-deps

# Now the minimal modules needed for a node only install

PySocks
pycryptodomex
requests
base58
# polysign is vendored in-tree (see polysign/); it is no longer a pip dependency.
# coincurve / ed25519 below are only needed for the optional non-RSA (ECDSA/ED25519) signers.
# coincurve
# ed25519
